const intialState = []
const usersReducer = (state = intialState, action) => {
    switch (action.type) {

        default: {
            return [...state]
        }
    }
}

export default usersReducer